[phases.setup]
nixPkgs = ["xorg.libxcb", "libGL", "glib"]
aptPkgs = ["libxcb1", "libxcb-render0", "libxcb-shape0", "libxcb-xfixes0", "libgl1-mesa-glx", "libglib2.0-0", "libsm6", "libxext6", "libxrender1"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
